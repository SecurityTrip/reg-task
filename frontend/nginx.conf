server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /styles.css {
        root /usr/share/nginx/html;
    }

    location /script.js {
        root /usr/share/nginx/html;
    }
}
    
  